{
  "name": "yan-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "yan's human soft path: @clack/prompts collects answers, then runs `yan <cmd>` with full flags. People only - agents take the hard path and never need node.",
  "engines": {
    "node": ">=20.12.0"
  },
  "dependencies": {
    "@clack/prompts": "1.7.0"
  }
}
